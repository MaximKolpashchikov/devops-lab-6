---
name: lint-ansible
on:
  push:
    branches:
      - master
jobs:
  build:
    name: Lint Ansible
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run ansible-lint
        uses: ansible/ansible-lint@main
